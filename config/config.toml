# Honcho config.toml — local Ollama (light) + Ollama Cloud (heavy)
# Copy to: $APPDATA_ROOT/config/config.toml
# Mounted read-only into the api container.
#
# PROVIDER STRATEGY:
#   "vllm"  = Local Ollama on Unraid tower (qwen3.5:4b)
#             → used for: deriver, summary, dialectic minimal + low
#   "custom" = Ollama Cloud subscription (llama4)
#             → used for: dialectic medium + high + max, dream
#   Embeddings = Local Ollama (embeddinggemma, 768d)

[app]
LOG_LEVEL = "INFO"
SESSION_OBSERVERS_LIMIT = 10
GET_CONTEXT_MAX_TOKENS = 100000
MAX_FILE_SIZE = 5242880
MAX_MESSAGE_SIZE = 25000
EMBED_MESSAGES = true
MAX_EMBEDDING_TOKENS = 8192
NAMESPACE = "honcho"

[db]
CONNECTION_URI = "postgresql+psycopg://honcho:honcho@database:5432/honcho"
SCHEMA = "public"
POOL_SIZE = 10
MAX_OVERFLOW = 20
POOL_TIMEOUT = 30
POOL_RECYCLE = 300

[auth]
USE_AUTH = false

[cache]
ENABLED = true
URL = "redis://redis:6379/0?suppress=true"
DEFAULT_TTL_SECONDS = 300

# ---------------------------------------------------------------------------
# LLM providers
# ---------------------------------------------------------------------------
[llm]
DEFAULT_MAX_TOKENS = 2500
EMBEDDING_PROVIDER = "ollama"

# Embedding key, URL, and model are set via env vars (LLM_EMBEDDING_*)
OPENAI_COMPATIBLE_BASE_URL = "https://ollama.com/v1"

# ---------------------------------------------------------------------------
# Deriver — observation extraction (runs every message, needs speed + tools)
# ---------------------------------------------------------------------------
[deriver]
ENABLED = true
WORKERS = 1
POLLING_SLEEP_INTERVAL_SECONDS = 1.0
STALE_SESSION_TIMEOUT_MINUTES = 5
PROVIDER = "vllm"
MODEL = "qwen3.5:4b"
BACKUP_PROVIDER = "custom"
BACKUP_MODEL = "llama4"
DEDUPLICATE = true
MAX_OUTPUT_TOKENS = 4096
THINKING_BUDGET_TOKENS = 1
MAX_INPUT_TOKENS = 23000
WORKING_REPRESENTATION_MAX_OBSERVATIONS = 100
REPRESENTATION_BATCH_MAX_TOKENS = 1024
FLUSH_ENABLED = false

# ---------------------------------------------------------------------------
# Dialectic — multi-level reasoning for peer.chat
# ---------------------------------------------------------------------------
[dialectic]
MAX_OUTPUT_TOKENS = 8192
MAX_INPUT_TOKENS = 100000
HISTORY_TOKEN_LIMIT = 8192
SESSION_HISTORY_MAX_TOKENS = 4096

[dialectic.levels.minimal]
PROVIDER = "vllm"
MODEL = "qwen3.5:4b"
BACKUP_PROVIDER = "custom"
BACKUP_MODEL = "llama4"
THINKING_BUDGET_TOKENS = 1
MAX_TOOL_ITERATIONS = 1
MAX_OUTPUT_TOKENS = 250

[dialectic.levels.low]
PROVIDER = "vllm"
MODEL = "qwen3.5:4b"
BACKUP_PROVIDER = "custom"
BACKUP_MODEL = "llama4"
THINKING_BUDGET_TOKENS = 1
MAX_TOOL_ITERATIONS = 5

[dialectic.levels.medium]
PROVIDER = "custom"
MODEL = "llama4"
BACKUP_PROVIDER = "vllm"
BACKUP_MODEL = "qwen3.5:4b"
THINKING_BUDGET_TOKENS = 1
MAX_TOOL_ITERATIONS = 2

[dialectic.levels.high]
PROVIDER = "custom"
MODEL = "llama4"
BACKUP_PROVIDER = "vllm"
BACKUP_MODEL = "qwen3.5:4b"
THINKING_BUDGET_TOKENS = 1
MAX_TOOL_ITERATIONS = 4

[dialectic.levels.max]
PROVIDER = "custom"
MODEL = "llama4"
BACKUP_PROVIDER = "vllm"
BACKUP_MODEL = "qwen3.5:4b"
THINKING_BUDGET_TOKENS = 1
MAX_TOOL_ITERATIONS = 10

# ---------------------------------------------------------------------------
# Summary — session summarization
# ---------------------------------------------------------------------------
[summary]
ENABLED = true
MESSAGES_PER_SHORT_SUMMARY = 20
MESSAGES_PER_LONG_SUMMARY = 60
PROVIDER = "vllm"
MODEL = "qwen3.5:4b"
BACKUP_PROVIDER = "custom"
BACKUP_MODEL = "llama4"
MAX_TOKENS_SHORT = 1000
MAX_TOKENS_LONG = 4000
THINKING_BUDGET_TOKENS = 1

# ---------------------------------------------------------------------------
# Dream — memory consolidation (runs rarely, needs best quality)
# ---------------------------------------------------------------------------
[dream]
ENABLED = true
DOCUMENT_THRESHOLD = 50
IDLE_TIMEOUT_MINUTES = 60
MIN_HOURS_BETWEEN_DREAMS = 8
ENABLED_TYPES = ["omni"]
PROVIDER = "custom"
MODEL = "llama4"
BACKUP_PROVIDER = "vllm"
BACKUP_MODEL = "qwen3.5:4b"
MAX_OUTPUT_TOKENS = 16384
THINKING_BUDGET_TOKENS = 1
MAX_TOOL_ITERATIONS = 20
HISTORY_TOKEN_LIMIT = 16384
DEDUCTION_MODEL = "llama4"
INDUCTION_MODEL = "llama4"

[peer_card]
ENABLED = true

# embeddinggemma uses 768 dimensions — MUST match the model
[vector_store]
TYPE = "pgvector"
DIMENSIONS = 768

[metrics]
ENABLED = false

[telemetry]
ENABLED = false

[sentry]
ENABLED = false
